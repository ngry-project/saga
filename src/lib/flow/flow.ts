
/**
 * Represents a flow within each related {@link IFlowCommand}s are being executed and
 * {@link IFlowEvent}s are being handled.
 */
// tslint:disable-next-line:no-empty-interface
export interface IFlow {
}
